{
  "project_id": 7,
  "suite_id": 2,
  "suite_slug": "integration-api-tests",
  "platform": "web",
  "engine": "api",
  "target_url": "http://localhost:8080/",
  "api_base_url": "http://localhost:8080/",
  "test_credentials": {
    "password": "QaTool2026!Seed",
    "username": "qatooladmin",
    "displayName": "QA Admin"
  },
  "auth_setup_file": "tests/auth-setups/project-7.setup.ts",
  "helpers_dir": "helpers/project-7",
  "plans": [
    {
      "tc_id": 72,
      "filename": "tc-72-post-api-v2-time-timesheets-rejects-line-items-with-negative.md",
      "markdown": "# TC-72: POST /api/v2/time/timesheets rejects line items with negative or non-numeric hours\n\n<!-- source: qa-tool test case 72 | type: api -->\n\n## Scenario: TC-72 — POST /api/v2/time/timesheets rejects line items with negative or non-numeric hours\n\nStarting state: no browser, no page, no storageState. Use the `request` fixture directly against the configured baseURL.\n\nSteps:\n1. Authenticate as an employee\n2. Send a POST request to /api/v2/time/timesheets with a line item containing a negative hours value (e.g., -3)\n3. Record the HTTP status code and response body\n4. Send a POST request with a line item containing a non-numeric hours value (e.g., 'abc')\n5. Record the HTTP status code and response body\n\nExpect: Both requests return HTTP 400 (or 422) with a clear validation error identifying the invalid hours value; no timesheet record is created in either case\n"
    }
  ]
}