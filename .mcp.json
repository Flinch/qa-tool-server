{
  "mcpServers": {
    "playwright-test": {
      "command": "npx",
      "args": [
        "playwright",
        "run-test-mcp-server"
      ]
    },
    "maestro": {
      "command": "maestro",
      "args": [
        "mcp",
        "--no-viewer"
      ],
      "env": {
        "TEST_USER_NAME": "${TEST_USER_NAME}",
        "TEST_USER_PASSWORD": "${TEST_USER_PASSWORD}"
      }
    },
    "device-status": {
      "command": "node",
      "args": [
        "scripts/device-status-mcp-server.js"
      ]
    }
  }
}